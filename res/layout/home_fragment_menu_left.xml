<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/x300"
        android:layout_weight="3"
        android:background="@color/white"
        android:orientation="horizontal">

        <ListView
            android:id="@+id/home_fragment_menu_left_group"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:cacheColorHint="@color/transparent"
            android:background="@color/gray_bg"
            android:fadingEdge="none"
            android:layout_weight="1"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:listSelector="@drawable/gray_list_item_selector"
            android:scrollbars="none"/>

        <ListView
            android:id="@+id/home_fragment_menu_left_child"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:cacheColorHint="@color/transparent"
            android:fadingEdge="none"
            android:layout_weight="1"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:listSelector="@drawable/gray_list_item_selector"
            android:scrollbars="none"/>
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/gray_bg"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/x20"
        android:background="@color/white"/>

    <View
        android:id="@+id/home_fragment_menu_left_cover_view"
        android:layout_width="match_parent"
        android:layout_height="@dimen/x100"
        android:layout_weight="1"
        android:background="@color/popup_main_background"/>

</LinearLayout>